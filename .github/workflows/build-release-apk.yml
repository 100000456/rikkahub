name: Build Release APK

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/build-release-apk.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Patch release build identity
        run: |
          set -u
          python3 ci/patch_release.py > .ci-patch-release.log 2>&1 && echo "PATCH_OK" >> .ci-patch-release.log || echo "PATCH_FAILED" >> .ci-patch-release.log
          cat .ci-patch-release.log
          git config user.name "ci-bot"
          git config user.email "ci-bot@users.noreply.github.com"
          git add -A
          git commit -m "正式包：占开发版的包名和签名，可以直接盖上去" || true
          git push origin HEAD:master || (git pull --rebase origin master && git push origin HEAD:master)
          grep -q 'PATCH_OK' .ci-patch-release.log

      - name: Restore Android debug keystore
        uses: actions/cache@v4
        with:
          path: ~/.android/debug.keystore
          key: android-debug-keystore-v1

      - name: Align security settings page reference
        run: |
          set -u
          FILES=$(grep -rl 'SettingSecurityPage' app/src/main/java || true)
          echo "--- files referencing SettingSecurityPage ---"
          echo "$FILES"
          if [ -n "$FILES" ]; then
            echo "$FILES" | xargs sed -i 's/SettingSecurityPage/SecuritySettingPage/g'
          fi
          echo "--- after fix (RouteActivity) ---"
          grep -n 'SecuritySettingPage' app/src/main/java/me/rerere/rikkahub/RouteActivity.kt | head -20 || true
          echo "--- after fix (leftover wrong name) ---"
          grep -rn 'SettingSecurityPage' app/src/main/java | head -20 || true
          git config user.name "ci-bot"
          git config user.email "ci-bot@users.noreply.github.com"
          git add -A
          git commit -m "fix: 安全设置页引用名对齐（SettingSecurityPage -> SecuritySettingPage）" || true
          git push origin HEAD:master || (git pull --rebase origin master && git push origin HEAD:master)

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: web-ui/pnpm-lock.yaml

      - name: Install web-ui dependencies
        working-directory: web-ui
        run: |
          mkdir -p ../ci-logs
          pnpm install --frozen-lockfile 2>&1 | tee ../ci-logs/pnpm.log

      - name: Gradle cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-release-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}

      - name: Restore signing keystore
        uses: actions/cache@v4
        with:
          path: ci/keystore.jks
          key: keystore-${{ github.run_id }}
          restore-keys: |
            keystore-

      - name: Prepare signing config
        run: |
          mkdir -p ci
          if [ ! -f ci/keystore.jks ]; then
            keytool -genkeypair -keystore ci/keystore.jks -storetype JKS \
              -storepass rikkahubselfbuild -keypass rikkahubselfbuild \
              -alias rikkahub -keyalg RSA -keysize 2048 -validity 10000 \
              -dname "CN=RikkaHub Self Build"
          fi
          printf 'storeFile=%s/ci/keystore.jks\n' "$GITHUB_WORKSPACE" > local.properties
          printf 'storePassword=rikkahubselfbuild\n' >> local.properties
          printf 'keyAlias=rikkahub\n' >> local.properties
          printf 'keyPassword=rikkahubselfbuild\n' >> local.properties
          sed -e 's/storePassword=.*/storePassword=***/' local.properties

      - name: Write placeholder google-services.json
        run: |
          mkdir -p app ci-logs
          cat > app/google-services.json <<'JSON_EOF'
          {
            "project_info": {
              "project_number": "123456789012",
              "firebase_url": "https://rikkahub-placeholder.firebaseio.com",
              "project_id": "rikkahub-placeholder",
              "storage_bucket": "rikkahub-placeholder.appspot.com"
            },
            "client": [
              {
                "client_info": {
                  "mobilesdk_app_id": "1:123456789012:android:0000000000000000000000",
                  "android_client_info": {
                    "package_name": "me.rerere.rikkahub.debug"
                  }
                },
                "oauth_client": [],
                "api_key": [
                  {
                    "current_key": "AIzaSyDplaceholder000000000000000000000"
                  }
                ],
                "services": {
                  "appinvite_service": {
                    "other_platform_oauth_client": []
                  }
                }
              }
            ],
            "configuration_version": "1"
          }
          JSON_EOF

      - name: Write gradle init script
        run: |
          mkdir -p ci
          cat > ci/selfuse.gradle <<'EOF'
          gradle.projectsEvaluated {
            rootProject.allprojects {
              tasks.matching { it.name.startsWith('uploadCrashlyticsMappingFile') }.configureEach {
                enabled = false
              }
            }
          }
          EOF

      - name: Build release APK
        run: |
          chmod +x gradlew
          set -o pipefail
          ./gradlew :app:assembleRelease --stacktrace --init-script ci/selfuse.gradle 2>&1 | tee ci-logs/gradle-release.log

      - name: Dump failure info
        if: failure()
        run: |
          {
            echo "=== release failure at $(date -u) ==="
            echo "--- compile errors ---"
            grep -E "^e: " ci-logs/gradle-release.log 2>/dev/null | head -60 || true
            echo "--- pnpm tail ---"; tail -n 40 ci-logs/pnpm.log 2>/dev/null || true
            echo "--- gradle tail ---"; tail -n 500 ci-logs/gradle-release.log 2>/dev/null || true
          } > .ci-failure-release.txt
          git config user.name "ci-bot"
          git config user.email "ci-bot@users.noreply.github.com"
          git add -f .ci-failure-release.txt
          git commit -m "ci: dump release failure log" || true
          git push origin HEAD:master || true

      - name: Publish release APK
        if: success()
        uses: softprops/action-gh-release@v2
        with:
          tag_name: release-build
          name: Release Build
          prerelease: true
          body: |
            自用正式包，来源 commit ${{ github.sha }}

            基于 RikkaHub 源码，包名和签名跟开发版一致，可以直接覆盖安装，数据不丢。
          files: app/build/outputs/apk/release/*.apk

# rebuild marker: 2026-09-21T00:50+08:00 正式版：固定签名缓存 + 能挑格式的发文件
